# Core ML + CV (Python 3.13.1)
numpy>=2.1,<2.3
protobuf>=4.25,<6
opencv-python>=4.10,<4.14
mediapipe>=0.10.30,<0.11

# Optional ML backend (app can run with reduced gesture recognition if missing)
tensorflow>=2.20,<2.21

# Project runtime
flask>=3.0,<4
pyautogui>=0.9.54
pygetwindow>=0.0.9
pynput>=1.7.7
pyttsx3>=2.90
SpeechRecognition>=3.10.0
psutil>=5.9,<8

# Model training / fallback utilities
scipy>=1.14,<2
scikit-learn>=1.6,<2

# Windows voice extras used by pyttsx3 on Windows
comtypes>=1.4.8; platform_system == "Windows"

# Optional microphone input (requires PortAudio system library)
# pyaudio>=0.2.14

# Testing
pytest>=8.0,<9
